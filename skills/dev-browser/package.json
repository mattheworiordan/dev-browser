{
  "name": "dev-browser",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "dev-browser": "./dist/cli.mjs"
  },
  "imports": {
    "@/*": "./src/*"
  },
  "scripts": {
    "build": "esbuild scripts/start-server.ts scripts/start-relay.ts scripts/start-external-browser.ts --bundle --platform=node --format=esm --outdir=dist --external:playwright --external:express --external:hono --external:@hono/* && npm run build:cli",
    "build:cli": "esbuild src/cli.ts --bundle --platform=node --format=esm --outfile=dist/cli.mjs --external:playwright",
    "start-server": "node dist/start-server.js",
    "start-server:dev": "npx tsx scripts/start-server.ts",
    "start-extension": "node dist/start-relay.js",
    "start-extension:dev": "npx tsx scripts/start-relay.ts",
    "dev": "npx tsx --watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.7",
    "@hono/node-ws": "^1.2.0",
    "express": "^4.21.0",
    "hono": "^4.11.1",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.2",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.0.0"
  }
}
